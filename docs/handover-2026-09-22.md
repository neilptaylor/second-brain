# Session handover, 22 September 2026

Start here. Read this, then `knowledge/systems-map.md` and `docs/handover-systems-gaps.md`.

## What this repo is

`~/my second brain` is Neil's single home for Me & My Old Man. Git repo, pushed to `github.com/neilptaylor/second-brain`. Before today it was a README and five empty folders. It now holds 23 skills, the voice profile, the voice-of-customer system, 22 scheduled automations, confirmed pricing and a map of the whole business.

Neil works only in Claude Code and Cowork on the desktop. He does not use the web app. Explain technical things in plain language, the way you would to a bright eleven-year-old. He is a poet who learned business, not an engineer, and he will spot hedging.

## What was done today

- Pulled skills, voice profile and VoC in from three scattered homes: the Claude.ai synced folder, the Drive "Mega Claude Cowork/About Me" folder, and local `~/.claude`.
- Symlinked all 23 skills from `skills/` into `~/.claude/skills/`, so **Claude Code now runs the repo copies**. Edits here are live.
- Corrected pricing. It had been recorded as a £1,800 to £4,000 ladder, which was wrong.
- Audited every stage of the business against the skills and automations supporting it.
- Renamed the GitHub repo from my-brain to second-brain and pointed the local remote at it.

## Unfinished, pick these up first

### 1. Cowork is being retired. Claude Code only

Decided 22 September 2026. Neil is moving off Cowork and working only in Claude Code, with this repo as the root folder.

This resolves what would otherwise have been the biggest problem here. Skills lived in three independent places: this repo (symlinked into `~/.claude/skills/`, which is what Code reads), Cowork's own separate install, and the Claude.ai synced folder. Edits here reach Code instantly and never reached the other two. Cowork's copies had already silently lost seven skills, with a documented wipe and no known cause.

Dropping Cowork leaves one source of truth and nothing to sync. Verified safe before the decision: all 23 skills load in Code, and all 13 live scheduled tasks run from `~/.claude/scheduled-tasks` on the Code side, so none of them depend on Cowork.

Nothing to do here unless Neil hits something he used to do in Cowork and cannot do in Code. If that happens, the old install is still on disk at `~/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/` and nothing has been deleted.

### 2. One commit is unpushed

A one-line documentation note is committed locally but the push was blocked by a safety classifier. Run `git push` from the repo. Trivial, but worth clearing so local and GitHub match.

### 3. README pricing is still wrong

`README.md` is an excellent identity document and its narrative should be preserved. But it describes Bronze £1,750, Silver £3,800 and Gold £8,000. Those tiers do not exist. Live pricing is options A to D in `knowledge/offer/pricing.md`. Rewrite the offer section of the README to match, keeping Neil's voice.

## The main work: four gaps in the business

Full detail in `docs/handover-systems-gaps.md`. In short, the business runs in stages in `Shared drives/Systems` and four of them have no system behind them.

**Enquiry is the priority.** Awareness has nine skills. Sale has four. Enquiry has none, and it is the narrowest point in the funnel. Nothing drafts or qualifies a first reply, and nothing is sent between the Discovery call and the Sales Call. Neil's own plan for that gap is a price-reveal VSL at T-3 days, framed as "Watch this before we chat." It has never been built.

**Collections is an empty folder.** No invoice trigger, no chase, no deposit terms, no failed-payment path. Xero is connected and untouched by any skill. Do not design this alone. Deposit versus payment plan, and what happens when a family goes quiet mid-project, are Neil's decisions.

**Post production is dead.** Two documents from early 2025. Everything between the editor delivering and the family receiving is in Neil's head. This is an extraction job before it is a building job.

**Strategy is storage.** Campaigns, Core, Grief and Book are folders. Nothing connects the quarter to what the week actually contains.

Underneath those sit two structural problems: eight scheduled tasks named after individual prospects, which should be one follow-up engine reading the CRM, and five overlapping onboarding skills that need untangling before anything is built on them.

Do Enquiry first, on its own, and finish it. Four half-built stages is worse than one finished one.

## Two smaller things noticed

- `skills/form-response-to-doc/` contains a stray `SKILL (1).md` duplicate that should be checked and removed.
- Every SOP in `Systems/05 Onboarding` is still named Bronze, Silver or Gold. They describe an offer Neil no longer sells.
