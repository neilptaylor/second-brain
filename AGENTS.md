# AGENTS.md

This repo's working instructions live in `CLAUDE.md`. Read that file and follow it in full.

`AGENTS.md` is the tool-neutral name for the same thing, read by coding agents other than Claude Code (Cursor, Codex, Copilot, Jules and others). Keeping one set of rules in `CLAUDE.md` and pointing here means there is never a second, stale copy to drift out of date.
